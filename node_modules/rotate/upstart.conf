# Install:
#
# sudo cp upstart.conf /etc/init/rot13.conf
# sudo start rot13
#
# Remove:
#
# sudo rm /etc/init/rot13.conf

description "rotate.js server"
author "Andrew Pennebaker <andrew.pennebaker@gmail.com>"

start on startup
stop on shutdown

script
	export HOME="/root"
	exec node /srv/d_hactar/usr/src/node-rotate/rotate.js -s 2>&1
end script
